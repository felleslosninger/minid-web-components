import {
  Meta,
  Title,
  IconGallery,
  IconItem,
  Canvas,
  Controls,
  Story,
  Description,
} from '@storybook/addon-docs/blocks';
import '../components/icon/icon.component';
import {systemIcons} from '../components/icon/system.icon-library';
import * as IconStories from './Icon.stories';

<Meta of={IconStories} />

# Icon

<Description />
<Canvas of={IconStories.Main} />
<Controls of={IconStories.Main} />

## System icons

TODO: add icons

<IconGallery>
  {Object.keys(systemIcons).map((iconName) => (
    <IconItem key={iconName} name={iconName}>
      <mid-icon style={{ fontSize: '38px' }} name={iconName}></mid-icon>
    </IconItem>
  ))}
</IconGallery>
