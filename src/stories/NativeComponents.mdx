import { Meta, Canvas, Title, Stories } from '@storybook/addon-docs/blocks';
import * as stories from './NativeComponents.stories';

<Meta of={stories} />

<Title />

Sometimes you don't need the full power of web components. Som elements can be styled
using native HTML elements and utility classes.

<Canvas of={stories.Button} />

<Canvas of={stories.Input} />

<Canvas of={stories.Link} />
