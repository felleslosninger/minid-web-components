{
  "name": "minid-elements-demo",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "rimraf node_modules/.vite/ && vite --force",
    "build": "rimraf dist && tsc && vite build"
  },
  "dependencies": {
    "lit": "^3.1.2",
    "@digdir/designsystemet-theme": "^0.15.3",
    "@felleslosninger/minid-elements": "../"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "rollup": "^4.2.0",
    "typescript": "^5.0.2",
    "vite": "^5.1.7"
  }
}
